using UnityEngine;

namespace Platinum.Bot
{
    public class BotStatus : MonoBehaviour
    {
        public int Rank = 0;
        public bool Boss = false;

        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}
